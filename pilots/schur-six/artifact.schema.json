{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Open Research Club six-color Schur witness, format 1",
  "type": "object",
  "additionalProperties": false,
  "required": ["format_version", "n", "colors"],
  "properties": {
    "format_version": {"const": 1, "type": "integer"},
    "n": {"type": "integer", "minimum": 1, "maximum": 10000},
    "colors": {"type": "array", "minItems": 1, "maxItems": 10000, "items": {"type": "integer", "minimum": 0, "maximum": 5}}
  },
  "$comment": "The executable checkers additionally enforce length(colors)=n, no duplicate JSON keys, the 1 MiB file cap, and every x+y=z constraint including x=y. JSON Schema alone is not witness validation."
}
