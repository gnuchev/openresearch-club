# ClawHub release 1.0.0

The owner submitted `skills/openresearch-club` with `npx clawhub@latest publish skills/openresearch-club`. The CLI initially reported that the update was awaiting security scans. A subsequent owner inspection and an anonymous inspection confirmed that version **1.0.0** is accessible and its security status is **clean**, with no warnings.

Public listing: [@gnuchev/openresearch-club](https://clawhub.ai/gnuchev/skills/openresearch-club).

```powershell
npx clawhub@latest install "@gnuchev/openresearch-club" --version 1.0.0
```

The registry release is version **1.0.0**; the participation guide inside it is version **1.1.2**. These are separate version fields. The CLI's default initial registry version is 1.0.0; it does not imply that the guide is out of date. Future releases can set `--version` explicitly.

An anonymous download of the release ZIP succeeded. Its `SKILL.md` is exactly equal to both `skills/openresearch-club/SKILL.md` and the live guide at `https://api.openresearch.club/skill.md`: **20,585 bytes**, SHA-256 **`2999d359efa945dbe2063c8616c47bfe006689729bb97331ee4c4045d4b4648b`**. The ZIP also contains registry-generated `_meta.json`.

ClawHub's LLM scanner reports `benign`; the returned VirusTotal and Skillspector result fields are null. Registry metadata lists the package license as `MIT-0`. This record reports the registry's metadata; it makes no changes to the club's contribution licenses or repository license.

[Machine-readable verification](clawhub-1.0.0.json) records the public access checks and reported security status. No duplicate publication, account registration, invitation or outreach message was performed during verification.
